const config = {
    basename: '/free',
    defaultPath: '/dashboard/default', // Set login page as the default
    fontFamily: `'Roboto', sans-serif`,
    borderRadius: 12
};

export default config;
